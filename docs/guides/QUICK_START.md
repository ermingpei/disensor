# 如何安装到模拟器（快速指南）

## ✅ 正确的命令

### 方法1：直接指定模拟器（推荐）
```bash
flutter run -d C7DB9421-665B-4F60-B812-33B550C415A3
```

### 方法2：交互式选择（注意选对）
```bash
flutter run

# 当提示选择设备时：
[1]: SM A037W (192.168.1.152:5555)           ← Android真机
[2]: iPhone 16 Plus (C7DB9421...)            ← ✅ 这个是模拟器！
[3]: iPhone XII (00008101-000A60A60E11...)   ← iPhone真机
[4]: iPhone 12 (00008101-001854C61105...)    ← iPhone真机

# 输入：2 
# 然后按Enter
```

---

## 🎯 如何区分模拟器和真机

| 设备名称 | 类型 | 标记 |
|---------|------|------|
| iPhone 16 Plus | **模拟器** | (simulator) |
| iPhone XII (wireless) | 真机 | (wireless) |
| iPhone 12 (wireless) | 真机 | (wireless) |
| SM A037W | 真机 Android | IP地址 |

**关键词：**
- ✅ `(simulator)` = 模拟器
- ❌ `(wireless)` = 真机
- ❌ `IP地址` = 真机

---

## 🚀 快速命令（复制粘贴）

### iOS 模拟器（最快）
```bash
# 方式1：设备ID
flutter run -d C7DB9421-665B-4F60-B812-33B550C415A3

# 方式2：设备名
flutter run -d "iPhone 16 Plus"
```

### Android 模拟器（如果有）
```bash
flutter run -d emulator-5554
```

### 真机（需要传感器时）
```bash
# iPhone XII
flutter run -d 00008101-000A60A60E11001E

# iPhone 12
flutter run -d 00008101-001854C61105001E

# Samsung
flutter run -d 192.168.1.152:5555
```

---

## 💡 Pro Tip：创建别名

在 `~/.zshrc` 或 `~/.bash_profile` 中添加：

```bash
# Flutter 快捷命令
alias fsim='flutter run -d C7DB9421-665B-4F60-B812-33B550C415A3'
alias f12='flutter run -d 00008101-000A60A60E11001E'
alias fxii='flutter run -d 00008101-001854C61105001E'
alias fand='flutter run -d 192.168.1.152:5555'
```

然后：
```bash
source ~/.zshrc
fsim  # 直接启动模拟器！
```

---

## 🔄 热重载快捷键

运行后，在终端按键：
- **r** - 热重载（保留状态）
- **R** - 热重启（重置状态）
- **o** - 切换性能叠加层
- **p** - 显示网格
- **q** - 退出

---

## ✅ 现在正在做什么

我已经为您在 **iPhone 16 Plus 模拟器** 上启动了应用！

等待编译完成（约15-20秒），然后您就能看到：
1. 封面页（onboarding）
2. 修复后的 Dashboard（有加载指示器）
3. 不再黑屏！

**下次直接运行：**
```bash
flutter run -d "iPhone 16 Plus"
```

或者设置别名后：
```bash
fsim
```
