# ✅ 黑屏问题已解决 (Dashboard Black Screen Fixed)

## � **问题根源 (Root Cause)**

### 1. **RenderBox Crash (Main Issue)**
- **症状**: 进入 Dashboard 后黑屏，控制台报错 `RenderBox was not laid out` 和 `BoxConstraints(unconstrained)`.
- **原因**:  在 `DebugDashboard` 的 `Row` 组件中使用了 `crossAxisAlignment: CrossAxisAlignment.baseline`，但处于布局约束不确定的环境中（可能是 `Flexible` + `FittedBox` 组合导致的约束计算失败）。Flutter 无法在未确定高度的情况下计算基线对齐。
- **影响**: 整个渲染树崩溃，导致屏幕无法绘制任何内容（黑屏）。

### 2. **Onboarding 丢失**
- **症状**: 用户反馈欢迎界面变成了简单的单页或空白。
- **原因**: 为了调试 Dashboard，我们曾临时简化了 `OnboardingPage`，但在恢复时可能出现了版本回退不完整的情况。

---

## 🛠 **解决方案 (Fix Implemented)**

### 1. **修复布局崩溃 (Fix Layout Crash)**
- **文件**: `lib/features/debug_dashboard.dart`
- **操作**: 
    - ✅ 将所有 `CrossAxisAlignment.baseline` 替换为 **`CrossAxisAlignment.end`**（底部对齐是基线对齐的安全替代方案）。
    - ✅ 将顶层的 `SingleChildScrollView` + `Column` 替换为更稳健的 **`ListView`**（自动处理滚动和垂直约束）。
    - ✅ 移除了可能导致宽/高无限计算的嵌套 `FittedBox` 约束风险。

### 2. **恢复欢迎界面 (Restore Onboarding)**
- **文件**: `lib/features/onboarding_page.dart`
- **操作**: 
    - ✅ 完全重写文件，恢复了包含3页滑动引导（Decentralized Sensing, Proof of Coverage, Earn QBIT）的完整版本。
    - ✅ 确保 `PageView` 的 `itemBuilder` 使用安全的布局。

---

## 📲 **验证步骤 (Verification)**

1.  **启动 App**: 看到 DiSENSOR Logo 和 3页引导教程。
2.  **点击 NEXT**: 可以滑动浏览所有页面。
3.  **点击 ENTER NETWORK**: 
    - ✅ 成功跳转 Dashboard。
    - ✅ 没有黑屏。
    - ✅ 能够看到 QBIT 收益卡片、网络状态、传感器数据。
4.  **调试控制台**: 没有 `RenderBox was not laid out` 红色报错。

---

## ⚠️ **后续建议**

- 以后在使用 `CrossAxisAlignment.baseline` 时，务必确保 `Row` 的高度是确定的，并且所有子元素都已经 Layout 完成。如果不确定，使用 `end` 或 `center` 是更安全的选择。
- `ListView` 是替代 `SingleChildScrollView(child: Column(...))` 的首选方案，特别是在列表项较多时，性能更好且约束更清晰。
